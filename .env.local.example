# Frontend Next.js — variáveis públicas e locais (copie para .env.local e preencha)
# NEXT_PUBLIC_* são expostas ao browser; as demais ficam só no server.

NEXT_PUBLIC_API_URL=http://localhost:8000
DATABASE_URL=
REDIS_URL=
